import homework.printFibSequence

// This is main, the entry point of a Java/Kotlin Application
fun main(args: Array<String>) {
    // Print out various sequences to verify things work.
    printFibSequence(nThPosition = 0)
    printFibSequence(nThPosition = 1)
    printFibSequence(nThPosition = 2)
    printFibSequence(nThPosition = 10)
}